kw1 - SELECT imie, kolor, cena FROM klienci WHERE topKlient LIKE 1;
kw2 - SELECT imie, cena FROM klienci WHERE termin LIKE '2024-05-23%';
kw3 - SELECT klienci.kolor, klienci.imie, ksztalty.ksztalt FROM klienci JOIN ksztalty ON klienci.id=ksztalty.id WHERE ksztalty.ksztalt LIKE 'migdał';
kw4 - ALTER TABLE `wzory` ADD `doplata` INT NOT NULL AFTER `wzor`;
